{
  "name": "studywithjesus-functions",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "functions:install": "rm -rf node_modules package-lock.json && npm install",
    "build": "npm run lint"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.3.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
