/**
 * Firebase Configuration Example
 * 
 * IMPORTANT: DO NOT commit your actual Firebase credentials to the repository.
 * 
 * How to use this file:
 * 1. Copy this file to config/firebase.config.js (without .example)
 * 2. Replace the placeholder values with your actual Firebase project credentials
 * 3. Add config/firebase.config.js to your .gitignore file
 * 
 * For production deployments:
 * - Use environment variables or GitHub Actions secrets
 * - Consider using Firebase Hosting's reserved URLs for automatic configuration
 * 
 * To get your Firebase configuration:
 * 1. Go to the Firebase Console (https://console.firebase.google.com/)
 * 2. Select your project (or create a new one)
 * 3. Click the gear icon (Settings) > Project settings
 * 4. Scroll down to "Your apps" and select your web app (or create one)
 * 5. Copy the firebaseConfig object values
 */

// Firebase configuration object
// Replace these placeholder values with your actual Firebase project credentials
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  // Optional: measurementId for Google Analytics
  // measurementId: "G-XXXXXXXXXX"
};

// Leaderboard configuration
// These settings control how the leaderboard feature behaves
window.LEADERBOARD_CONFIG = {
  // Set to true to enable Firebase integration
  firebaseEnabled: true,
  
  // Alternative: REST API backend URL (set to null to use Firebase or sample data)
  // backendUrl: "https://your-api-server.com/api",
  backendUrl: null,
  
  // Number of top scores to display per module
  topN: 10,
  
  // Available modules (update if modules change)
  modules: ['270201', '270202', '270203', '270204'],
  
  // Module display names
  moduleNames: {
    '270201': 'Engine Systems',
    '270202': 'Driveline & Drivetrain',
    '270203': 'Hydraulics & Air Brakes',
    '270204': 'Electrical, Auto Body & Mobile Equipment'
  }
};

/**
 * SECURITY BEST PRACTICES
 * =======================
 * 
 * 1. Firestore Security Rules
 *    Configure Firestore security rules to prevent unauthorized access.
 *    See docs/leaderboard-design.md for example rules.
 * 
 * 2. Firebase Authentication
 *    For admin features, require Firebase Auth with custom claims.
 *    Set admin claim via Firebase Admin SDK in a Cloud Function.
 * 
 * 3. API Key Restrictions
 *    In Google Cloud Console, restrict your API key to:
 *    - Specific referrers (your domain)
 *    - Only the APIs you use (Firestore, Auth)
 * 
 * 4. Rate Limiting
 *    Use Cloud Functions or Firestore rules to limit write frequency.
 * 
 * 5. Data Validation
 *    Always validate data on the server/in security rules.
 *    Don't trust client-side validation alone.
 */
